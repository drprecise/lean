Predicted intended commands: 9

Planned next actions:
- Fetch the four provider source datasets only after explicit approval.
- Normalize each provider into a paired deliverable set: one machine-readable JSON and one human-readable TXT.
- Keep raw provider data and derived outputs separate.
- Produce audit/provenance artifacts that record source endpoints, filtering rules, counts, and transformation notes.

Acceptance criteria:
- Each provider has a paired JSON + TXT output.
- JSON remains machine-oriented and minimally mutated.
- TXT remains clean, human-readable, and value-only where appropriate.
- Audit/provenance records are present and traceable to source inputs.
- No provider data is fetched until the next task explicitly authorizes it.

Source-of-truth assumptions:
- Gains, Drift, Parcl, and Polymarket are the only four providers in scope.
- Provider-specific schemas may differ; normalization must preserve provider identity and exact asset identifiers.
- Availability semantics must be explicit when a source distinguishes open, active, closed, archived, or accepting-orders states.
- Any closed-world schema decisions should be derived from the fetched source data, not guessed in advance.
